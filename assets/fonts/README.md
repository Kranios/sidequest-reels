# Fonts
Drop the real reel fonts here (same as the app uses):
- Raleway-Black.ttf   (wordmark + hooks)
- Raleway-Bold.ttf
- Raleway-Regular.ttf

Until present, compose.py falls back to a default font so the pipeline still
runs, but the output will not be brand-accurate. Install before real production.
